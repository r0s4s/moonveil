## Security Notes

- Never commit real credentials to `config.ini`. 
  Copy `.env.example` to `.env` and set your own `SECRET_KEY`.
- This tool is for authorized security testing only.
  See `LICENSE` for full disclaimer.
